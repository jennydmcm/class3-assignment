import { StyleSheet } from "react-native";

const stylesHome = StyleSheet.create({
    buttonArea: {
        display: "flex",
        flexDirection: "row",
        gap: 4

    },

})
export default stylesHome;